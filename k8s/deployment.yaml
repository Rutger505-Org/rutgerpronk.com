apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio
  template:
    metadata:
      labels:
        app: portfolio
    spec:
      containers:
        - name: portfolio
          image: rutger505/portfolio:1.1.1
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: portfolio
            - secretRef:
                name: portfolio